name: Build Android APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install deps
        run: npm install

      - name: Build web bundle
        env:
          VITE_SUPABASE_URL:       ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY:  ${{ secrets.VITE_SUPABASE_ANON_KEY }}
        run: npm run build

      - name: Add Android platform if missing
        run: |
          if [ ! -d android ]; then
            npx cap add android
          fi

      - name: Capacitor sync
        run: npx cap sync android

      - name: Patch Java target to 17
        run: |
          if [ -f android/app/build.gradle ]; then
            sed -i 's/JavaVersion\.VERSION_21/JavaVersion.VERSION_17/g' android/app/build.gradle || true
          fi

      - name: Install WebView crash-recovery MainActivity
        run: |
          MAIN=$(find android/app/src/main/java -name MainActivity.java | head -1)
          PKG=$(sed -n 's/^package \(.*\);/\1/p' "$MAIN")
          sed "s/__PACKAGE__/$PKG/" native-android/MainActivity.java > "$MAIN"
          echo "Installed crash-recovery MainActivity at $MAIN (package $PKG)"
          cat "$MAIN"

      - name: Build debug APK
        working-directory: android
        run: |
          chmod +x ./gradlew
          ./gradlew assembleDebug --no-daemon

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: mage-dm-dashboard-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
